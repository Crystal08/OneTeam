<%= form_for(@request_skill) do |f| %>
  <% if @request_skill.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@request_skill.errors.count, "error") %> prohibited this request_skill from being saved:</h2>

      <ul>
      <% @request_skill.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :skill_id %><br />
    <%= f.number_field :skill_id %>
  </div>
  <div class="field">
    <%= f.label :request_id %><br />
    <%= f.number_field :request_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
