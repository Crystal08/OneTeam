<p>
  <b>Employee:</b>
  <%= @selection.name %>
</p>

<p>
  <b>Request:</b>
  <%= Request.find(@selection.request_id).task %>
</p>

<p>
  <b>Notes:</b>
  <%= @selection.notes %>
</p>

<%= link_to 'Edit', edit_selection_path(@selection) %> |
<%= link_to 'Back', employee_requests_path(@current_employee) %>
