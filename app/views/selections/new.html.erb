<div class="textarea">
<h1>Select developer</h1>

</br>
<p><em><%=@response.employee.first_name %> <%=@response.employee.last_name %></em> for Project: <em><%= @response.request.title %></em></p>

<%= form_for([@response,@selection]) do |f| %> 

  <%= render 'form', :f => f %></br>
  
  <hr>
  <%= f.submit "Select This Employee", class: "btn btn-med" %>

<% end %>

<%= link_to 'Back', employee_requests_path(current_employee) %>
</div>
